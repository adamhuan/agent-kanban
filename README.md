# Agent Kanban Board

一个实时展示 Agent 任务状态的看板应用，使用 Next.js 15 + TypeScript + Tailwind CSS 构建。

## 功能特性

### 🎯 核心功能
- **实时任务看板**：包含「待办」、「进行中」、「已完成」三个状态列
- **Agent 强关联**：每个任务都明确关联到具体的 Agent
- **拖拽管理**：支持拖拽任务卡片到不同状态列
- **实时更新**：系统每30秒自动更新任务状态

### 📊 数据展示
- **Agent 状态面板**：显示所有 Agent 的在线状态和当前任务
- **任务统计**：展示任务总数、完成率、时间统计等关键指标
- **进度可视化**：每个任务都有进度条，直观展示完成情况
- **标签系统**：任务支持多标签分类

### 🚀 技术栈
- **Next.js 15**：React 全栈框架，App Router
- **TypeScript**：类型安全的 JavaScript
- **Tailwind CSS**：实用优先的 CSS 框架
- **@hello-pangea/dnd**：漂亮的拖拽库
- **Lucide React**：精美的图标库

## 快速开始

### 安装依赖
```bash
npm install
```

### 开发模式
```bash
npm run dev
```

访问 http://localhost:3000

### 生产构建
```bash
npm run build
npm start
```

## 项目结构

```
agent-kanban/
├── src/
│   ├── app/
│   │   ├── page.tsx          # 主页面
│   │   ├── layout.tsx        # 根布局
│   │   └── globals.css       # 全局样式
│   ├── components/
│   │   ├── TaskCard.tsx      # 任务卡片组件
│   │   ├── KanbanColumn.tsx  # 看板列组件
│   │   ├── AgentStatus.tsx   # Agent 状态组件
│   │   └── StatsPanel.tsx    # 统计面板组件
│   ├── lib/
│   │   └── data.ts           # 模拟数据服务
│   └── types/
│       └── index.ts          # TypeScript 类型定义
├── public/                   # 静态资源
└── package.json
```

## 数据模型

### Task（任务）
```typescript
interface Task {
  id: string;
  title: string;
  description: string;
  status: 'todo' | 'in_progress' | 'done';
  agentId: string;           // 关联的 Agent ID
  agentName: string;         // Agent 名称
  createdAt: Date;
  updatedAt: Date;
  priority: 'low' | 'medium' | 'high' | 'critical';
  tags: string[];
  progress?: number;         // 0-100
  estimatedTime?: number;    // 预估时间（分钟）
  actualTime?: number;       // 实际时间（分钟）
}
```

### Agent（代理）
```typescript
interface Agent {
  id: string;
  name: string;
  avatar?: string;
  status: 'online' | 'offline' | 'busy';
  currentTask?: string;      // 当前任务ID
  tasksCompleted: number;    // 已完成任务数
}
```

## 实时更新机制

应用实现了两种更新方式：

1. **自动更新**：每30秒随机更新一个任务的进度和状态
2. **手动拖拽**：用户可以通过拖拽改变任务状态
3. **手动刷新**：点击刷新按钮立即更新

## 扩展建议

### 后端集成
- 连接 WebSocket 实现真正的实时通信
- 集成数据库（如 PostgreSQL、MongoDB）
- 添加用户认证和权限管理

### 功能增强
- 任务详情页面
- 任务评论和协作功能
- 时间追踪和报告
- 通知系统
- 移动端适配

### 部署选项
- Vercel（推荐）
- AWS Amplify
- Docker 容器化部署
- 自托管服务器

## 许可证

MIT License